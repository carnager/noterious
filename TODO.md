- pasting urls should automatically create markup
- the rename rightclick option, as well as renaming the title of a note, should allow to use folder names to move files. 
- markdown in tables and lists are not working correctly. in tables they dont do anything and in lists/tasks/headings (and maybe elsewhere) the markup is shown
- tasks section in tree view should have some filter options like "Not Done" "has reminders" or similar. Default should probably show all not done. 
- tab key is not working while editing. it jumps to elements instead of tabbing
- when font size is increased sections in sidepanel are cut off
