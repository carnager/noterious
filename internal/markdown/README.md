# Markdown Layer

This package will own:

- page parsing
- task extraction
- frontmatter extraction
- semantic rewrites back to markdown

The long-term goal is to support semantic server-side edits without making clients manipulate markdown with ad-hoc regexes.
